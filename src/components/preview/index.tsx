import React from 'react';
import { Box } from '@material-ui/core';

const PreView: React.FC = () => {
  const classes = () => {
  };

  console.log(classes);

  return (
    <Box>
      Preview
    </Box>
  );
};

export default PreView;
